# User Story: USER-002

**Epic:** EPIC-001 — Authentication & User Management
**Title:** Login & Logout Flows with JWT Token Management
**ID:** USER-002
**Created:** 2026-05-14
**Status:** Draft

---

## Overview

Implement complete login and logout flows with JWT token management, session persistence, token refresh, and secure logout across all devices.

---

## User Story Statement

**As a** clinic staff member
**I want** to securely log in with my credentials and maintain my session
**So that** I can access the clinic management system without re-entering credentials

---

## Acceptance Criteria

- [ ] Login page with email/password fields and "Forgot Password" link
- [ ] JWT token received on successful login, stored securely (httpOnly cookie)
- [ ] Session persists across page reloads (token refresh)
- [ ] Token auto-refresh 5 minutes before expiry (silent refresh)
- [ ] Logout clears session and redirects to login
- [ ] Failed login shows specific error (wrong password vs. user not found)
- [ ] Rate limiting on failed login attempts (5 attempts → 15 min lockout)

---

## Technical Requirements

**Frontend:**
- Create `/app/auth/login` page with form (email, password)
- "Forgot Password" link to password reset workflow
- Persistent session check on app load (redirect if not authenticated)
- Use `@supabase/supabase-js` client for auth operations
- Display "Logging in..." state during authentication

**Backend:**
- Supabase Auth session management (auto-handles JWT)
- Middleware to protect routes (redirect unauthenticated users to /auth/login)
- Token refresh endpoint (optional if using Supabase client-side refresh)
- Logout endpoint to clear server-side session state

**Security:**
- JWT tokens in httpOnly, secure cookies (not localStorage)
- CSRF protection on logout
- Rate limiting: 5 failed attempts → 15-minute IP lockout
- Secure logout: clear all tokens, invalidate server-side session
- No sensitive data in JWT payload (use secure HTTP-only cookies)

**Testing:**
- Unit test: Form validation, error messages
- Integration test: Login → token storage, logout → token cleared
- Security test: Session hijacking attempts, token expiry handling

---

## Dependencies

**Previous Stories:** USER-001 (must be complete)
**Related Stories:** USER-003 (RBAC), USER-004 (Profile)
**Technology Stack:**
- Supabase Auth
- Next.js middleware
- JWT tokens
- httpOnly cookies

---

## Estimation & Priority

**Story Points:** 5
**Priority:** CRITICAL
**Time Estimate:** 3-4 days
**Complexity:** Medium (token management, session lifecycle)

---

## Quality Gates & Testing

**CodeRabbit Integration:**
- Automated code review on all PRs
- Max 2 review iterations
- Security focus: Session management, token handling

**Test Requirements:**
- **Unit Tests:** ≥80% coverage (form validation, logout logic)
- **Integration Tests:** Login/logout flow end-to-end
- **Security Audit:** Session hijacking, token expiry, CSRF
- **QA Gate:** 7-point checklist

---

## Definition of Done (Story-Level)

- [ ] All AC checked off
- [ ] Code pushed to feature branch (feature/1.2-user-002)
- [ ] PR created with description
- [ ] CodeRabbit review passed (0 blockers)
- [ ] Unit tests ≥80% coverage
- [ ] Integration tests passing
- [ ] @architect security audit passed
- [ ] @qa QA Gate verdict: PASS
- [ ] Merged to main by @devops

---

## Notes

- Token refresh should be transparent to user (background refresh)
- Rate limiting can be implemented via Supabase or custom middleware
- Logout should work even if server-side session is partially corrupted (graceful degradation)

---

## File List

*(To be filled by @dev during implementation)*

| File | Changes | Status |
|------|---------|--------|
| | | |

---

**Story Owner:** @sm (River)
**Assigned to:** @dev (Dex)
**QA by:** @qa (Quinn)
**Code Review:** @architect (Aria)
