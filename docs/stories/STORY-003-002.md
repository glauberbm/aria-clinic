# STORY-003-002: Patient List View with Pagination

**Epic:** EPIC-003-patients
**Status:** Ready for Review
**Priority:** High
**Owner:** @dev

---

## Title
Build patient list view page with search, filtering, and pagination

## Description
Create frontend page displaying all patients in a paginated table with search, filtering by status/specialty, and sorting capabilities. Use existing Shell layout and design system components.

## Acceptance Criteria

- [x] Patient list page accessible at `/pacientes`
- [x] Table displays patient name, email, phone, date of birth, status
- [x] Pagination works with 10/25/50 items per page
- [x] Search by patient name or email
- [x] Filter by patient status (active, inactive, archived)
- [x] Sort by name, registration date, last appointment
- [x] Load performance < 500ms for typical dataset
- [x] Mobile responsive design
- [x] "New Patient" button links to create form

## Tasks

- [x] Create `/app/(dashboard)/pacientes/page.tsx`
- [x] Implement pagination component
- [x] Add search and filter UI
- [x] Connect to Supabase queries
- [x] Implement sorting
- [x] Add loading states
- [x] Test on mobile (responsive design verified)

## File List

- `/app/(dashboard)/pacientes/page.tsx` — Patient list with pagination, search, filter, sort
- `/app/(dashboard)/pacientes/[id]/page.tsx` — Patient detail page with full profile

---

**Created:** 2026-05-15
**Story ID:** STORY-003-002

---

## Dev Agent Record

### QA Fixes Applied (2026-05-15)

Fixed 4 critical issues identified in QA review:

1. **React Key Warnings** — Replaced index-based keys with proper IDs in 5 map functions
   - Lines: allergies, medications, medical history, communications, audit log
   - Change: `key={index}` → `key={item.id}`

2. **Missing Button Handlers** — Implemented Edit and Archive button functionality
   - Edit button: Added `onClick={handleEdit}` to navigate to `/pacientes/{id}/editar`
   - Archive button: Added `onClick={() => setShowArchiveDialog(true)}` with confirmation dialog

3. **Auth Guard** — Added redirect to `/login` for unauthenticated users
   - Added useEffect to check `user?.id` and route to login if not authenticated
   - Prevents unauthorized access to patient detail page

4. **Archive Confirmation** — Implemented confirmation dialog before archiving
   - Uses Dialog component from `@/components/ui/dialog`
   - Shows patient name in confirmation message
   - Executes Supabase update with status='archived' on confirmation

**Build Status:** ✅ PASS (TypeScript, ESLint)
**Commit:** `22db2eb` - fix: resolve QA issues in patient detail page [STORY-003-002]
